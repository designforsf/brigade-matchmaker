<%= form_with(model: category_tag, local: true) do |form| %>
  <% if category_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(category_tag.errors.count, "error") %> prohibited this category_tag from being saved:</h2>

      <ul>
        <% category_tag.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :category_id %>
    <%= form.text_field :category_id %>
  </div>

  <div class="field">
    <%= form.label :tag_id %>
    <%= form.text_field :tag_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
