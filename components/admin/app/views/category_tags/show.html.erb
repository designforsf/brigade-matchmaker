<p id="notice"><%= notice %></p>

<p>
  <strong>Category:</strong>
  <%= @category_tag.category_id %>
</p>

<p>
  <strong>Tag:</strong>
  <%= @category_tag.tag_id %>
</p>

<%= link_to 'Edit', edit_category_tag_path(@category_tag) %> |
<%= link_to 'Back', category_tags_path %>
