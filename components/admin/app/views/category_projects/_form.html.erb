<%= form_with(model: category_project, local: true) do |form| %>
  <% if category_project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(category_project.errors.count, "error") %> prohibited this category_project from being saved:</h2>

      <ul>
        <% category_project.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
